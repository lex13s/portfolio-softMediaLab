
# Test React Task

Implement a control based on the provided layout using **React**, **TypeScript**, **Sass**, **Bootstrap**, and **redux-form**.  
Pixel-perfect implementation is not required, but the result should look very similar to the provided image.
``

In the project directory, you can run:

### `yarn start`

Runs the app in the development mode.<br />
Open [http://localhost:3000](http://localhost:3000) to view it in the browser.

Deploy - https://objective-feynman-46f41d.netlify.app/

Design - https://ibb.co/71Qzjdb

